# .env.example - example environment variables for EaglesMusic
# Copy to .env and fill with real values (do NOT commit secrets)

# Django settings
DJANGO_SECRET_KEY=replace-with-a-secure-random-string
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=yourusername.pythonanywhere.com,localhost,127.0.0.1

# Optional: if you use an external DB instead of SQLite
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/NAME

# Optional email settings (if you plan to send email)
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password
# EMAIL_USE_TLS=True

# Notes:
# - Keep your real SECRET_KEY and any passwords out of git. Use the PythonAnywhere Web tab
#   environment variables or a secure secrets manager to set production values.
